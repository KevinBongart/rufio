<div id="header">
  <h1><%= link_to "Rufio", root_path %></h1>

  <div class="right">
    <%= render 'filters' if @filters %>
    <%= render 'select_environment' if @environment && !@environment.new_record? %>
  </div>
</div>
